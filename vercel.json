{
    "builds": [{
        "src": "weatherapp/wsgi.py",
        "use": "@vercel/python",
        "config": {"maxLambdaSize": "15mb", "runtime":"Python 3.12.5"}
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest": "weatherapp/wsgi.py"
        }
    ]
}